version: "3.9"

services:
  backend:
    image: glamora-backend
    build: .
    env_file:
      - .env
    ports:
      - "5001:5000"
    volumes:
      - .:/app          # Mount local code inside container
      - /app/node_modules
    command: npx nodemon --legacy-watch src/server.ts